package com.com.riteship.campus.recruitment.systems.standalone.application;

//@TestConfiguration(proxyBeanMethods = false)
public class TestRiteShipCampusRecruitmentSystemsOriginalStandAloneApplication {

	/*@Bean
	@ServiceConnection
	KafkaContainer kafkaContainer() {
		return new KafkaContainer(DockerImageName.parse("confluentinc/cp-kafka:latest"));
	}

	@Bean
	@ServiceConnection
	MariaDBContainer<?> mariaDbContainer() {
		return new MariaDBContainer<>(DockerImageName.parse("mariadb:latest"));
	}

	@Bean
	@ServiceConnection
	MongoDBContainer mongoDbContainer() {
		return new MongoDBContainer(DockerImageName.parse("mongo:latest"));
	}

	@Bean
	@ServiceConnection
	MySQLContainer<?> mysqlContainer() {
		return new MySQLContainer<>(DockerImageName.parse("mysql:latest"));
	}

	public static void main(String[] args) {
		SpringApplication.from(RiteShipCampusRecruitmentSystemsOriginalApplication::main).with(TestRiteShipCampusRecruitmentSystemsOriginalStandAloneApplication.class).run(args);
	}
*/
}
